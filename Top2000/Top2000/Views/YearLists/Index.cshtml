@model IEnumerable<Top2000.Models.YearList>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.YearListYear)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.YearListYear.Year)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.YearListId }) |
            @Html.ActionLink("Details", "Details", new { id=item.YearListId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.YearListId })
        </td>
    </tr>
}

</table>
